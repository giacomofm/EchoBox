meta {
  name: echo BANDWIDTH_LIMIT_EXCEEDED
  type: http
  seq: 509
}

get {
  url: {{baseUrl}}/echo?status=509
  body: none
  auth: inherit
}

params:query {
  status: 509
}

assert {
  res.status: eq 509
}

settings {
  encodeUrl: true
  timeout: 0
}
